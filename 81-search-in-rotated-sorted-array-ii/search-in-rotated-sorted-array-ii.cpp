class Solution {
public:
    bool search(vector<int>& nums, int target) {
        int idx=0;
        vector<int> newarr;
        bool ist=false;
           for(auto it:nums) {
            if(it==target) {
return true;
            }
           }

           return false;
           
    }
};